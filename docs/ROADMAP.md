# Roadmap — Quests

Meilensteine für EmberRougeLike, abgeleitet aus `docs/GDD.md`. Jede Quest ist ein spielbarer/testbarer Zuwachs, keine reine Code-Aufräumarbeit. Reihenfolge folgt den Prioritäten aus dem GDD (Movement/Dash zuerst, dann Combat, dann Räume, dann Progression, Bosse und Prozeduralität zuletzt).

## ✅ Quest 0: Erwachen
Grundbewegung (8-Richtungen, normalisiert, Screen-Clamping). **Bereits erledigt.**

## ✅ Quest 1: Der erste Schritt zur Seite (Dash)
Dash mit fixer Distanz, Cooldown und Invincibility-Frames (siehe GDD 1.2). **Bereits erledigt** — fühlt sich laut Playtest gut an.
- Vorbereitend: Das aktuelle libGDX-Default-Logo wird durch eine einfache flache PNG-Platzhalter-Grafik für den Spieler ersetzt (siehe GDD 5) — kein echtes Sprite, aber ein echter Entity-Platzhalter statt des Engine-Logos.
- Fertig wenn: Spieler kann dashen, ist während des Dashs kurz unverwundbar, Cooldown verhindert Spam.
- Backlog (nicht blockierend): visuelle Cooldown-Anzeige für den Dash — bewusst auf später verschoben.

## ✅ Quest 2: Erste Klinge
Eine Nahkampfwaffe mit eigenem Angriffsmuster, Hitbox-Treffererkennung, ein Dummy-Gegner zum Testen. **Bereits erledigt.**
- Fertig wenn: Spieler kann angreifen, Treffer wird erkannt, Dummy-Gegner nimmt Schaden.
- Umgesetzt: `Sword` per Komposition (`Player` hat-ein `Sword`), Maus-Aim mit Y-Flip-Korrektur, Distanz-basierte Hitbox/Hurtbox-Trennung gegen `Enemy`-Dummy.
- Bewusst vereinfacht (siehe Roadmap-Kommentar in `Player.update()`): `Enemy` wird aktuell als einzelner Parameter durchgereicht, nicht als Liste — wird generalisiert, sobald Quest 4-6 mehrere Gegner gleichzeitig braucht.

## ✅ Quest 3: Erster Pfeil
Eine Fernkampfwaffe (Projektil-System), Wechsel zwischen Nah-/Fernkampf ist hier noch nicht nötig (nur 1 Waffenslot laut GDD) — stattdessen Testumgebung mit beiden Waffentypen gleichzeitig (Schwert links, Bogen rechts). **Bereits erledigt.**
- Fertig wenn: Projektil kann abgefeuert werden, trifft Gegner, wird sauber entsorgt (Objektlebenszyklus!).
- Umgesetzt: `Projectile` als eigenständige, sich über Zeit bewegende Entity (Position, Richtung, Geschwindigkeit, Lebensdauer/Reichweite, `hitsRemaining` als Vorbereitung für spätere Piercing-Effekte). `Bow` verwaltet eine `List<Projectile>` (sicheres Entfernen via `removeIf`), teilt sich eine einzige Textur statt pro Pfeil neu zu laden.
- `Weapon`-Interface aus `Sword` und `Bow` extrahiert (`update(deltaTime, target)`, `tryAttack(origin, direction, target)`) — bewusst nur die Methoden, die wirklich polymorph gleich genutzt werden; Zeichnen bleibt getrennt, da Sword nur Debug-Hitbox zeigt, Bow aber echte Pfeil-Sprites.

## ✅ Quest 4: Der erste Raum
Ein einzelner, in Tiled gebauter Raum mit Gegnern, Tür-Lock bis alle Gegner besiegt sind (siehe GDD 4). **Bereits erledigt.**
- Fertig wenn: Raum lädt aus Tiled-Datei, Tür öffnet sich erst nach Clear.
- Umgesetzt: `Room` lädt/rendert Tiled-Maps (`TmxMapLoader`/`OrthogonalTiledMapRenderer`), liest `EnemySpawn`/`Door`-Objekte aus der Objekt-Ebene. `Door` (rot/grün eingefärbte Platzhalter-Textur) verriegelt, solange die Gegner-Liste nicht leer ist.
- Nebenbei: Feste 640×360-Basisauflösung mit ganzzahlig skaliertem, letterboxtem Viewport (`GameConfig`), sauber auf 1080p/1440p/4K skalierbar; Fullscreen-Taste (`F11`); Gegner-Liste statt Einzel-Parameter über alle Waffen hinweg generalisiert.
- Gelernt: libGDX' `TmxMapLoader` flippt Objekt-Y-Koordinaten beim Laden bereits selbst — kein zusätzlicher manueller Y-Flip nötig (anders als bei der Maus-Position).

## ✅ Quest 5: Gegner, die zurückschlagen
Einfaches Gegner-KI-Grundgerüst (Verfolgen, Angreifen), Schadenssystem für den Spieler (Leben, Game Over). **Bereits erledigt.**
- Fertig wenn: Mindestens 1 Gegnertyp verfolgt/greift an, Spieler kann sterben.
- Umgesetzt: `EnemyState` (CHASING/ATTACKING) mit Hysterese (unterschiedliche Schwellenwerte zum Betreten/Verlassen von ATTACKING, verhindert Zittern an der Grenze). `Player` hat `health`/`isAlive()`/`takeDamage()` (respektiert `isInvincible()` vom Dash). `Main.startGame()` als wiederverwendbarer Aufbau, Game-Over-Zustand friert die Logik ein, `R` startet neu.
- Bewusst zurückgestellt: globale Spieler-Invincibility-Frames nach Treffer (verhindert aktuell nicht, dass mehrere Gegner im selben Frame gleichzeitig treffen) — Balancing-Frage für später.

## ✅ Quest 6: Der Pfad durch mehrere Räume
Mehrere handgebaute Räume, verbunden zu einem kleinen Level, Übergänge zwischen Räumen. **Bereits erledigt.**
- Fertig wenn: Spieler kann durch mind. 3-4 verbundene, handgebaute Räume laufen.
- Umgesetzt: `Door` um `targetRoom`/`targetDoorName` erweitert (Tür-Objekte per Namenskonvention verknüpft, siehe Quest 11), `Room.getDoorByName()`, `Door.getEntryPosition()` (Versatz ins Rauminnere anhand des Richtungs-Präfix im Türnamen, verhindert sofortiges Zurück-Triggern), `Main.switchRoom()` (Raum/Gegner komplett neu aufbauen, Spieler an der Zieltür zentrieren), scrollende, an die Raumgrenzen geklammerte Kamera (zentriert, wenn der Raum kleiner als der Viewport ist).
- Gefundene Bugs beim Testen mehrerer verbundener Räume: `Player`s Bewegungs-Clamping bezog sich fälschlich auf die feste Viewport-Größe (`GameConfig.WORLD_WIDTH/HEIGHT`) statt auf die tatsächliche Größe des aktuellen Raums (`Room.getPixelWidth()/getPixelHeight()`) — fiel erst auf, sobald ein Raum größer als der Viewport war. Zwei `.tmx`-Dateien hatten ihre Objekt-Ebene `"object"` statt `"Objects"` genannt (Tiled-Ebenenname wird von `Room` hart erwartet) → `NullPointerException` beim Laden.
- **Teilweise gelöst (2026-08-04, siehe Quest-11-Vorlauf):** `switchRoom()`/`enterRoom()` bauen den Zielraum zwar weiterhin jedes Mal frisch aus der `.tmx`-Datei auf, aber `Main.clearedRoomPaths` merkt sich pfad-basiert, welche Räume in diesem Run schon geleert wurden, und überspringt das Gegner-Spawnen dafür — Backtracking spawnt also keine besiegten Gegner mehr neu. Bewusst NICHT als persistenter Raum-Cache gelöst (`Room`-Objekte/Pickups bleiben weiterhin Wegwerf-Instanzen) und bewusst pfad- statt instanz-basiert — eine vollständig instanz-basierte Lösung (falls Quest 11 dieselbe Raum-Vorlage mehrfach pro Run wiederverwendet) bleibt weiterhin von der dortigen Entscheidung abhängig.

## ✅ Quest 7: Aus der Asche geschmiedet (eigener Art-Pass)
Der Kern-Loop (Movement, Dash, Combat, Räume) steht — Zeit, alle bisherigen Platzhalter-Grafiken durch eigene Pixelart zu ersetzen (siehe GDD 5).
- Fertig wenn: Spieler + 1 Enemy-Typ haben animierte Sprites (Idle/Walk/Attack/Hurt/Death), 1 Bodenset mit funktionierendem Dual-Grid (Boden + Wasser/Löcher) plus einfache Wände sind spielbar, Healthbar/UI hat einen verfeinerten (nicht-finalen) Pixel-Look. Waffen/Projektile/Türen bleiben bewusst Platzhalter.
- **Abgeschlossen mit dokumentierter Ausnahme (2026-07-21):** Enemy (Slime) hat alle 5 echten Zustands-Animationen, Terrain/Wände (Dual-Grid) und Healthbar sind fertig. Beim Player fehlen `Attack` und `Hurt` weiterhin (nur Idle-Süd/Nord, Walk-Süd/Nord, Death sind echte Kunst) - bewusst auf den zweiten Art-Pass verschoben, siehe Backlog-Eintrag "Waffenspezifische Attack-Animation" unten: der Nutzer möchte erst wissen, wie die Waffen aussehen (die Attack-Pose hängt visuell von der geführten Waffe ab), bevor er die Angriffs-/Treffer-Animation des Charakters selbst finalisiert.
- Scope-Entscheidung (vermerkt 2026-07-15, siehe GDD 5/6): bewusst als **Vertical Slice** angelegt — wenige Dinge bekommen Kunst, aber Animationssystem und Dual-Grid sollen strukturell schon so funktionieren wie in der finalen Version, nicht als Wegwerf-Lösung für später nochmal umgebaut werden.
- Stand 2026-07-16: Alle Systeme stehen und funktionieren mit Platzhaltern - Animation (Player + Enemy, alle 5 Zustände) und Dual-Grid-Terrain (isoliert bewiesen, dann in `Room`/`testmap.tmx` integriert). Es fehlt nur noch der echte Kunst-Tausch selbst (Aseprite, siehe GDD 5 für die Werkzeug-Korrektur) - technische Spezifikation dafür (Frame-Layouts, Sprite-Sheet-Maße, Tileset-Bitmask-Zuordnung) in `docs/ART_SPEC.md`.
- Technischer Vorlauf (vermerkt 2026-07-13, ergänzt 2026-07-15):
  - **Pixel-Snapping:** Sprite-Zeichenposition beim `draw()` auf das nächste ganze logische Pixel runden (z.B. `Math.round(position.x)`), unabhängig von der eigentlichen (weiterhin fließkomma-genauen) Bewegungsberechnung — verhindert Wackeln/Shimmer an Sprite-Rändern bei skaliertem Nearest-Neighbor-Rendering.
  - **Dual-Grid System für Terrain-Übergänge:** Render-Gitter um eine halbe Kachel gegen das Daten-Gitter versetzt; jede Render-Kachel wird aus den 4 angrenzenden Daten-Zellen bestimmt (2⁴ = 16 Kombinationen, 16 Kachel-Grafiken reichen für lückenlose Übergänge, statt bis zu 47 bei klassischem Blob-Autotiling). Kein natives Tiled-Feature (anders als "Wang Sets") — müsste als eigener Rendering-Layer in unserem Code gebaut werden. Besonders wertvoll für Quest 11 (prozedurale Generierung), da Terrain-Typen dort zur Laufzeit von einem Algorithmus entschieden werden, nicht von Hand in Tiled vorgezeichnet. Für Quest 7 reicht 1 Terrain-Paar (Boden/Wasser-Löcher), um das System vollständig zu validieren.
  - **Tile-Extrusion/Bleeding:** Kleiner Pixel-Rand um jede Kachel im Tileset-Bild, verhindert sichtbare Nahtlinien zwischen Kacheln bei skaliertem Rendering.
  - **Bildschirmfeste UI-Kamera (neu, 2026-07-15):** Die Healthbar muss unabhängig von der scrollenden Welt-Kamera (seit Quest 6) immer an derselben Bildschirmposition bleiben. Braucht eine zweite, feste Kamera/Projektionsmatrix nur fürs UI (analog zum Batch/ShapeRenderer-Matrizen-Thema, aber diesmal zwei waschechte Kameras statt nur zwei Matrizen auf derselben Kamera) - UI wird damit in einem eigenen Zeichen-Durchgang mit dieser UI-Kamera gerendert, nicht mit `camera` aus `Main`.
  - **Dual-Grid-Rand-Überstand (gefunden 2026-07-16, Quest 7.9):** Das Render-Gitter ist immer eine halbe Kachel größer als das Daten-Gitter (jede Render-Kachel liegt zentriert auf einem Eck-Schnittpunkt, auch an den äußersten Ecken) - der Boden "blutet" dadurch prinzipbedingt um eine halbe Kachel über den Datenraster-Rand hinaus. Fällt normalerweise nicht auf (mehr Welt jenseits des sichtbaren Bereichs, oder Wände verdecken den Rand), wurde aber in `testmap.tmx` sichtbar, weil Raum- und Viewport-Größe fast identisch sind (kein Scroll-Spielraum) und die Wand-Kacheln exakt am Gitterrand liegen statt eine halbe Kachel davor. Bewusst nicht jetzt gefixt (Level-Design-Frage: Wände breiter anlegen oder Datenraster mit Rand-Puffer, sobald echte Kunst existiert) - für den Vertical-Slice-Nachweis der Dual-Grid-Technik selbst irrelevant.

**Backlog (vermerkt 2026-07-20, explizit für später): Healthbar animieren.** Aktuell zwei einfache statische Bilder (`healthbar_empty.png`/`healthbar_full.png`, kein Aseprite-JSON nötig). Soll später animiert werden (z.B. Puls-Effekt bei niedrigem Leben) - würde dann doch auf `AsepriteSpriteSheet` umgestellt werden müssen. Die Inhalts-Rand-Erkennung (`findContentLeftEdge()`/`findContentRightEdge()` in `Healthbar.java`, ermittelt den sichtbaren Bereich der "Full"-Grafik ohne transparenten Rand) müsste dafür nicht pro Frame neu laufen - Breite/Höhe direkt aus den JSON-Metadaten (`meta.size`) übernehmen, Rand-Erkennung nur einmalig anhand des ERSTEN Frames durchführen (Annahme: alle Frames teilen dieselbe sichtbare Silhouette, nur z.B. Farbe/Glanz ändert sich).

**Backlog (vermerkt 2026-07-20, explizit für später): Healthbar mit beliebiger Wipe-Form statt rechteckigem Ausschnitt.** Aktuelle Technik (`TextureRegion.setRegionWidth()`) kann nur rechteckig von links nach rechts aufdecken - für z.B. einen runden/kreisförmigen oder diagonalen Wipe-Effekt reicht das nicht. Zwei mögliche Wege: (1) **Shader-basierte Maskierung** (eigener GLSL-Fragment-Shader, vergleicht eine Masken-Textur/berechnete Form gegen einen Lebens-Anteil-Schwellenwert) - technisch sauber und stufenlos für JEDEN Prozentwert, aber komplettes Neuland für dieses Projekt (bisher keine eigenen Shader). (2) **Gestufte, einzeln gezeichnete Zustände** (z.B. 5-10 Füllstufen als separate Frames, ausgewählt nach Lebens-Anteil) - nutzt dieselbe Frame-Auswahl-Mechanik wie `Animation` bereits bietet (Index statt Zeit-basiert), aber sichtbare Sprünge statt fließendem Übergang, und mehr Zeichenaufwand. Shader = glatt aber neu/aufwändig, gestufte Frames = einfacher umzusetzen aber weniger geschmeidig.

**Backlog (vermerkt 2026-07-19, explizit für später, nicht Teil des aktuellen Scopes): Y-sortierte Render-Queue für Player + platzierbare Objekte, mit Verdeckungs-Transparenz.** Sobald es platzierbare Hindernis-Objekte gibt (z.B. Bäume), sollen diese nicht mehr in einer festen Ebene VOR oder HINTER dem Player gezeichnet werden, sondern gemeinsam mit dem Player in EINER Zeichen-Warteschlange, sortiert nach dem "Ursprungspunkt" jedes Objekts (der KLEINSTEN Y-Koordinate seiner Bounding-Box - also der Boden-/Fuß-Punkt, nicht die Mitte). Zeichenreihenfolge: von der größten Ursprungs-Y (am weitesten "hinten"/oben im Bild) zur kleinsten Ursprungs-Y (am weitesten "vorne"/unten im Bild) - klassisches Maler-Algorithmus-Prinzip. Der Player ist bewusst TEIL dieser Queue (nicht separat immer obenauf gezeichnet), kann also z.B. hinter einem Baum verschwinden, wenn er weiter oben im Bild steht als der Baum. Zusätzlich gewünscht: Wird der Player von einem Objekt verdeckt, soll er transparent/als Silhouette durchscheinen, damit der Spieler ihn nie aus den Augen verliert (verbreitete UX-Lösung in Top-Down-/Isometrie-Spielen für genau dieses Problem).

**Backlog (gefunden 2026-07-19, bewusst erst NACH dem aktuellen Art-Pass angehen): Spieler wirkt "aufgeklebt" statt Teil der Spielwelt.** Vermutlich vor allem fehlende Boden-Verankerung durch einen Schatten - ein weicher, halbtransparenter Blob unter den Füßen (nicht am Sprite-Mittelpunkt, sondern leicht versetzt) ist der wirksamste bekannte Trick dafür, größtenteils code-seitig lösbar (auch ohne eigene Schatten-Pixelart machbar, z.B. Pixmap-generiert). Zweiter, kleinerer möglicher Faktor: ob Lichtrichtung/Schattierung zwischen Charakter- und Terrain-Kunst zueinander passen - das wäre dann tatsächlich Sprite-seitig zu justieren. Schatten zuerst ausprobieren, bevor an der Schattierung selbst gearbeitet wird.

**Backlog (gefunden 2026-07-18, bewusst erst NACH dem aktuellen Art-Pass angehen): Spiegelungs-Qualität bei asymmetrischer Kunst.** `Player.draw()` spiegelt für "nach links" dieselben Frames einfach horizontal (siehe Quest 7.3), statt separate Links-Frames zu zeichnen - spart die Hälfte der Zeichenarbeit, kann bei asymmetrischem Charakter-Design (z.B. einseitiger Schwanz/Gliedmaßen) aber optisch "falsch" wirken, weil eine Spiegelung nicht dasselbe ist wie eine tatsächlich neu gezeichnete Gegenrichtung. Erst beurteilbar, sobald die echte Kunst weiter fortgeschritten ist - dann entscheiden, ob der Spiegel-Kompromiss tragbar ist oder echte Links-Frames nötig werden.

**Backlog (gefunden 2026-07-18, GELÖST 2026-07-21 in Sidequest 1): Kollisions-/Klammerungs-Box passt nicht zur echten Sprite-Silhouette.** Raumgrenzen-Klammerung (`Player.update()`) und Zentrum-Berechnung (`getCenter()`) nutzten die VOLLE `SPRITE_WIDTH`/`SPRITE_HEIGHT`-Bounding-Box (48×48) für die Terrain-Kollision - bei echter Kunst mit Transparenz-Rand fühlte sich das Wand-Abstandsgefühl dadurch ungenau an. Gelöst über einen eigenen `Hitbox`-Tag im Spritesheet (Nutzer-Idee, siehe ART_SPEC.md Abschnitt 6 und Sidequest 1 unten) - die Kollisionsbox wird per Alpha-Kanal-Scan direkt aus der Kunst abgeleitet, Fallback auf die volle Fläche solange der Tag fehlt. Die Raumgrenzen-Klammerung selbst (`position.x/y` gegen `0`/`roomWidth-SPRITE_WIDTH`) bleibt bewusst unverändert auf der vollen Sprite-Fläche - nur die WAND-Kollision nutzt die kleinere Box.

**Backlog (gefunden 2026-07-16, ausdrücklich NICHT Teil des aktuellen Quest-7-Vertical-Slice): Dekorativer Rahmen + Hintergrund für Letterboxing/kleine Räume.** Aktuell füllt `Main.updateViewport()` jeden Platz außerhalb der ganzzahlig skalierten 640×360-Spielfläche nur mit einer einfarbigen Fläche (`ScreenUtils.clear()`); dasselbe gilt für den toten Innenraum um kleine Räume, die laut `updateCamera()` zentriert statt gescrollt werden (`room.getPixelWidth()/Height() <= GameConfig.WORLD_WIDTH/HEIGHT`). Geplant: ein gemeinsames System für beide Fälle - dekorativer Rahmen plus Hintergrundgrafik (Nutzer hat bereits ein Design dafür im Kopf), umgesetzt über **9-Slice/9-Patch-Tiling** (4 feste Ecken, 4 gestreckte Kanten, 1 gestreckte Mitte - eine Grafik reicht für beliebige Rahmengrößen, ähnliches Ökonomie-Prinzip wie das Dual-Grid-System für Terrain). Bewusst gegen die Alternative entschieden, die logische Weltgröße (`GameConfig.WORLD_WIDTH/HEIGHT`) selbst dynamisch an den verfügbaren Bildschirmplatz anzupassen ("mehr Welt sehen") - das würde tief in Kamera-Klammerung, Spieler-Bewegungs-Klammerung und Gegner-KI-Reichweiten eingreifen, die aktuell alle von der festen Weltgröße ausgehen; deutlich größerer Umbau als eine rein präsentationsseitige Rahmen-Lösung.

**Backlog (gefunden 2026-07-21, Teil desselben zukünftigen UI-Art-Pass): Eigene Pixel-Font.** Text-Rendering läuft über libGDX' `BitmapFont` (siehe SQ1.4) - vorerst mit der eingebauten Standard-Schrift, rein für Debug-Zwecke. Für eine spätere eigene, handgezeichnete Pixel-Font empfohlener Ansatz (statt des offiziellen AngelCode/BMFont-`.fnt`-Formats, das für variable Zeichenbreiten/Kerning gedacht ist und ein Erzeugungs-Tool bräuchte): eine **monospace Pixel-Font als festes Raster** (analog zum Dual-Grid-Tileset-Prinzip - feste Rasterposition statt Tags, hier `index = zeichen - ' '` statt Bitmask), geschnitten über `TextureRegion.split()`, gezeichnet über eine kleine eigene `PixelFont`-Klasse mit fester Zeichen-Schrittweite. Kein neues technisches Konzept, reine Wiederverwendung des Grid-Slicing-Prinzips.
Zusätzlich gewünscht: Ein neues `GameSettings`-Feld (passend zur bestehenden Settings-Konvention, siehe `GameSettings.autoSwing`), mit dem der Spieler wählen kann, ob sich UI-Elemente (Healthbar etc., siehe `uiCamera` aus Quest 7.2) an der festen 640×360-Gameplay-Fläche orientieren (aktuelles Verhalten) oder an der tatsächlichen Fensterauflösung (damit UI z.B. im Rahmenbereich bei großen Fenstern sichtbar wäre, statt nur innerhalb der Spielfläche).

**Backlog (gefunden 2026-07-16, präzisiert 2026-07-21, erst für den zweiten Art-Pass mit Waffen/Projektilen relevant): Waffenspezifische Attack-Animation, deshalb Player-Attack/Hurt komplett zurückgestellt.** Aktuell zeigt `Player` für Schwert UND Bogen dieselbe eine Attack-Animation (`determineAnimationState()` prüft nur `sword.isAttackVisualActive() || bow.isAttackVisualActive()`, ohne zu unterscheiden welche Waffe). Passt eigentlich nicht zur "Movesets statt reiner Stats"-Philosophie aus GDD 2 (jede Waffe soll ihr eigenes Angriffsmuster haben) - das gilt bisher nur für Timing/Hitbox, nicht fürs Sprite. Technischer Ansatz vermutlich analog zum gerade gebauten facingUp-Muster (Quest 7, 2026-07-16): zusätzliches Feld für die zuletzt aktive Waffe, zwei separate Attack-Animationen (`swordAttackAnimation`/`bowAttackAnimation`), Auswahl in `getCurrentAnimation()`. Braucht eigene Sprite-Sheet-Frames pro Waffe, die es aktuell (Waffen bleiben in Quest 7 bewusst Platzhalter) noch nicht gibt.

Entscheidung 2026-07-21: Der Nutzer zeichnet deshalb bewusst NICHT jetzt schon eine generische Player-Attack-Animation (und im selben Zug auch nicht Hurt, da beide zum "wie sieht der Charakter im Kampf aus"-Gesamtbild gehören) - er möchte erst wissen, wie die finalen Waffen-Sprites aussehen, um sich den Charakter im Kontext vorstellen zu können, bevor er Zeit in eine möglicherweise wegwerfbare generische Version investiert. Player bleibt bis dahin bei Idle/Walk/Death als einzigen echten Animationen, Attack/Hurt nutzen weiterhin den eingefärbten Idle-South-Platzhalter (siehe `Player`-Konstruktor, `putStateAnimation()`).

## ✅ Sidequest 1: Feste Wände und ein Blick unter die Haube
Zwei Lücken, die beim Spieltest nach Quest 7 aufgefallen sind, unabhängig von Quest 8 angehbar (vermerkt 2026-07-21):

- **Fertig wenn:** Spieler UND Gegner können nicht mehr durch Wände (`WallData`) oder Terrain-Lücken/Wasser (`TerrainData`) laufen; ein Debug-Overlay (`DebugSettings`-Flag) zeigt die wichtigsten Spieler-Stats (Health/MaxHealth, Speed, Dash-Cooldown) als Text im Spiel an.

**SQ1.1 - Kollisionsabfrage in `Room` freilegen.** `DualGridRenderer` kennt sein `isSpecialCell`-Gitter bereits (dient aktuell nur dem Rendering) - bekommt eine neue öffentliche Methode, die eine WELT-Koordinate (nicht Gitter-Koordinate) entgegennimmt und intern durch `TILE_SIZE` teilt, dann die bestehende `isSpecialAt(gridX, gridY)`-Logik samt Clamp-to-Edge wiederverwendet. `Room` bekommt darauf aufbauend eine öffentliche `isBlocked(float worldX, float worldY)`, die `true` liefert, wenn entweder `wallRenderer` ODER `terrainRenderer` an dieser Stelle "speziell" ist (Wand bzw. Lücke/Wasser) - keine neue Daten-Kopie nötig, dieselbe Quelle wie das Rendering.

**SQ1.2 - Player gegen Terrain kollidieren lassen.** Statt Bewegung direkt auf `position` anzuwenden, X und Y **getrennt** prüfen (Achsen-getrennte Sliding-Kollision, Standard-Technik für einfache Tilemap-Kollision): neue X-Position berechnen, per `room.isBlocked()` an allen 4 Ecken der Kollisionsbox prüfen, nur übernehmen wenn frei - danach dasselbe für Y. Ergebnis: Spieler rutscht an Wänden entlang, statt bei diagonaler Bewegung komplett stehenzubleiben, sobald nur eine Achse blockiert ist. **Erweitert um Nutzer-Idee (2026-07-21):** Die Kollisionsbox selbst kommt nicht aus einer festen `SPRITE_WIDTH`/`SPRITE_HEIGHT`-Konstante, sondern wird aus einem eigenen `Hitbox`-Tag im Spritesheet abgeleitet (Alpha-Kanal-Scan, siehe ART_SPEC.md Abschnitt 6) - löst den separaten Quest-7-Backlog-Punkt "Kollisionsbox passt nicht zur echten Sprite-Silhouette" gleich mit, statt ihn auf später zu verschieben. Fallback auf die volle Sprite-Fläche, solange kein `Hitbox`-Tag existiert.
**SQ1.3 - Enemy gegen Terrain kollidieren lassen.** Exakt dieselbe Technik wie SQ1.2 (inkl. `Hitbox`-Tag-Ableitung), auf `Enemy.update()` angewendet (Verfolgungs-Bewegung statt Input-Bewegung, aber dieselbe Achsen-Trennung + `room.isBlocked()`-Abfrage) - sonst würde die KI weiterhin durch Wände zum Spieler laufen.

**SQ1.4 - Debug-Stat-Anzeige.** Neues `DebugSettings.renderStats`-Flag (gleiches Muster wie `renderHitboxes`/`renderTerrainDebug`). Text-Rendering ist neu im Projekt - libGDX' `BitmapFont` (Default-Konstruktor, kein eigenes Font-Asset nötig) im selben UI-Kamera-Zeichen-Durchgang wie die Healthbar. Zeigt vorerst Health/MaxHealth, Speed, Dash-Cooldown-Rest - bewusst nur Debug-Text, kein gestaltetes UI-Element (das käme, falls überhaupt gewünscht, erst in einem UI-Art-Pass).

**Backlog (gefunden 2026-07-21 beim Testen von SQ1.2/1.3, bewusst zurückgestellt): Tür ist durch die neue Wand-Kollision unerreichbar geworden.** `Door`-Objekte liegen in `testmap.tmx` innerhalb der Wand-Kachelreihe (passend zur alten, rein visuellen Wand ohne echte Kollision) - jetzt, wo Wände tatsächlich blockieren (Sidequest 1), kann der Spieler die Tür-Trigger-Zone nicht mehr erreichen. Nutzer-Entscheidung: NICHT jetzt einzeln flicken, sondern zusammen mit dem noch ausstehenden Tür-Art-Pass (Türen sind bisher reine Platzhalter, siehe Quest 7) neu positionieren/dimensionieren - beides hängt ohnehin zusammen (Tür-Kollisionsverhalten sollte zur finalen Tür-Kunst passen, nicht doppelt angepasst werden).

## ✅ Quest 8: Beute und Level-Ups
In-Run-Progression: Raum-Belohnungen und Level-Up-System (siehe GDD 3.1).
- Fertig wenn: Spieler bekommt nach Raum-Clear Belohnungsauswahl, kann durch XP/Ressourcen aufsteigen.
- **Stat-System (siehe GDD 2.1, entschieden 2026-07-21, umgesetzt 2026-07-21):** `PlayerStats` hält globale Stats (`damageMultiplier`, `attackSpeedMultiplier`, `critChance`, `critDamageMultiplier`, `projectileCount`, `cooldownReduction`, plus die vorerst ungenutzten Platzhalter `moveSpeed`/`lifeSteal`/`range`). `Weapon` (`Sword`/`Bow`) liefert einen `baseDamage`/`DamageType` (aktuell beide `PHYSICAL`), Schaden wird über `stats.computeDamage()` verrechnet (Multiplikator + Crit-Roll). `DamageModifier`-Interface als Erweiterungspunkt für typabhängige Effekte steht bereit, aber noch ohne konkrete Implementierung (kein Item braucht das aktuell) - siehe Task #45 zum späteren Nachholen als Lerninhalt.
- **Items + Belohnungsauswahl:** `Item`-Interface mit 5 konkreten Klassen (`DamageBoostItem`, `AttackSpeedBoostItem`, `CritChanceBoostItem`, `CritDamageBoostItem`, `ProjectileCountBoostItem`), `ItemPool` zieht zufällige Dreier-Auswahlen. `RewardChoiceUI` zeigt sie als Karten - Maus-Hover UND Tastatur (Pfeiltasten/A-D/Zahlentasten/Enter) steuern denselben Auswahl-Zustand (Nutzer-Entscheidung, erleichtert späteren Controller-Support). Kein Kunst-Pass, reine `ShapeRenderer`+`BitmapFont`-Darstellung (Vertical-Slice-Prinzip).
- **XP/Level:** `Enemy.xpValue` (Standard 25), `Player.addXp()` sammelt XP und meldet ausgelöste Level-Ups zurück; Schwelle wächst pro Level ×1.5.
- **XP als Pickup (präzisiert 2026-07-21, Nutzer-Entscheidung nach erstem Test):** Gegner geben XP nicht mehr sofort beim Tod, sondern droppen einen `XpPickup` (eigene `Pickup`-Basisklasse, Platzhalter-Grafik `xp_orb_placeholder.png`) an ihrer Position - erst bei Kontakt mit dem Spieler wird die XP gutgeschrieben (`Pickup.collect()`). `Pickup` ist bewusst generisch gehalten (Template-Method, `applyEffect()` XP-spezifisch nur in `XpPickup`), damit später 1-2 Meta-Währungen über eine eigene `Pickup`-Unterklasse laufen können, ohne die Basisklasse anzufassen. Level-Ups werden dabei über `player.getLevel()` vor/nach dem Einsammeln erkannt (nicht über einen Rückgabewert von `collect()`), damit `Pickup` selbst nichts Level-Up-Spezifisches wissen muss.
- **Trigger:** Level-Up UND Raum-Clear (alle Gegner besiegt, nur für Räume die welche hatten) lösen beide dieselbe Auswahlrunde aus - über eine Warteschlange, damit mehrere Level-Ups im selben Frame nicht verlorengehen. Während einer aktiven Auswahl pausiert die gesamte Gameplay-Logik (gleiches Prinzip wie das bestehende Game-Over-Gate). Raum-Clear prüft bewusst `enemies.isEmpty()` (nicht `!anyEnemyAlive` wie die Tür) - löst also erst aus, wenn der letzte Gegner nach seiner Sterbeanimation komplett entfernt ist, nicht schon im Moment des tödlichen Treffers.
- **Pickup-Magnet (präzisiert 2026-07-21, Nutzer-Feedback nach erstem Test):** `Pickup.update()` zieht Orbs innerhalb von `PlayerStats.pickupRange` (neuer, veränderbarer Stat, Standard 60) automatisch zum Spieler - behebt sowohl unerreichbar liegende Drops (z.B. an einer Wandkante) als auch das reine Kontakt-Sammeln, das sich unpräzise anfühlte.
- Offen für später: konkrete Item-Werte/-Liste sind ein erster grober Wurf, kein finales Balancing; `moveSpeed`/`lifeSteal`/`range`-Platzhalter haben noch keine Items.
- **Weitere vorgemerkte Stats (2026-07-21, noch nicht verdrahtet, siehe GDD 2.1):** `projectileSpeedMultiplier`, `xpMultiplier` (auf `XpPickup`-Erträge). `bounceCount` (Ricochet/Abprall für Projektile mit dieser Eigenschaft, über Items ODER spezifische Waffen) braucht zusätzlich noch ein eigenes Abprall-Verhalten in `Projectile` (z.B. nächstes Ziel suchen statt einfach zu verschwinden, Bounce-Zähler) - das ist reines Neuland und noch nicht designt, nicht nur eine fehlende Verdrahtung wie bei den beiden anderen.

**Backlog (gefunden 2026-07-21, bewusst zurückgestellt): Raum-Clear-Belohnung soll vom letzten Gegner gedroppt werden statt automatisch auszulösen.** Aktuell prüft `Main` nur `enemies.isEmpty()` und reiht dann automatisch eine Auswahlrunde ein - funktioniert, fühlt sich aber wie ein unsichtbarer Auto-Trigger an. Geplante Lösung: der letzte besiegte Gegner droppt zusätzlich einen Raum-Belohnungs-Gegenstand (später z.B. optisch als Kiste), den der Spieler wie einen Pickup einsammeln muss, um die Auswahl auszulösen - konsistent mit dem XP-Pickup-Prinzip, nur ohne den Magnet-Effekt (Raum-Belohnungen sollen laut Nutzer NICHT automatisch angezogen werden, nur XP/Ressourcen-Pickups).

## ✅ Quest 9: Der Hub
Meta-Progression zwischen Runs: permanente Währung + Hub-Menü für dauerhafte Upgrades, erste freischaltbare Inhalte (siehe GDD 3.2). **Abgeschlossen 2026-08-02.**
- Fertig wenn: Währung überlebt einen Run-Neustart, mind. 1 dauerhaftes Upgrade ist kaufbar.
- **Persistenz:** `MetaCurrencyType` (`UPGRADE`/`UNLOCK`), `MetaProgress` (Felder `upgradeCurrency`/`unlockCurrency`/`purchasedUpgradeIds`, zentrale `addCurrency()`-Schnittstelle für jede zukünftige Quelle) über libGDX' eingebaute `Json`-Klasse nach `Gdx.files.local("save.json")` gespeichert/geladen. `Main` lädt einmalig, hält die einzige Instanz, `Player` bekommt sie nur als Referenz. Gefundener libGDX-Bug: `Json` deserialisiert JSON-Arrays IMMER als `ArrayList`, egal welcher Feld-Typ deklariert ist - ein `Set<String>`-Feld crashte deshalb beim Laden (`IllegalArgumentException`); gelöst, indem `purchasedUpgradeIds` als `List<String>` statt `Set<String>` deklariert ist (Duplikat-Prüfung läuft stattdessen explizit beim Kauf).
- **CurrencyPickup + Drops:** EINE generische `CurrencyPickup`-Klasse (parametrisiert über `MetaCurrencyType` + Betrag) statt separater Klassen pro Währung - anders als bei `Item`, wo jede Klasse einen eigenen Stat-Effekt hat, unterscheiden sich Währungs-Pickups in nichts außer "welcher Zähler wird erhöht". Gegner droppen zusätzlich zu `XpPickup` mit 30%-Chance `UPGRADE`-Währung; Raum-Clear droppt zusätzlich einmalig `UNLOCK`-Währung (an Meilensteine gekoppelt, siehe GDD 3.1).
- **PermanentUpgrade-System:** `PermanentUpgrade`-Interface (`getId()`, `getDescription()`, `getCost()`, `getCurrencyType()`, `apply(Player player)`) - zielt bewusst auf `Player`, nicht nur `PlayerStats`, da Upgrades auch Felder wie `maxHealth` betreffen können, die außerhalb von `PlayerStats` liegen. `getId()` ist ein stabiler String-Schlüssel (kein Reflection-Klassenname, überlebt Refactorings) für `purchasedUpgradeIds`. Erstes Upgrade: `MaxHealthUpgrade` (`+25 Max-HP` für 50 `UPGRADE`). `PermanentUpgradePool` hält eine FESTE Liste (keine Zufallsauswahl wie bei `ItemPool` - der Shop zeigt alles gleichzeitig). `Player`s Konstruktor wendet beim Erstellen jedes neuen Runs alle in `purchasedUpgradeIds` vermerkten Upgrades erneut an, bevor `health = maxHealth` gesetzt wird - dadurch ist der Effekt wirklich permanent, ohne Run-lokale und persistente Daten zu vermischen.
- **UpgradeShopUI:** Eigene Klasse statt Wiederverwendung von `RewardChoiceUI` - zeigt ALLE verfügbaren Upgrades gleichzeitig (nicht 1-von-3), bleibt offen bis der Spieler sie schließt (Escape oder erneuter Tastendruck der Interaktionstaste), erlaubt mehrere Käufe nacheinander. Karten farbcodiert (Grau = bereits gekauft, Rot = nicht leistbar, Gold/Grün = auswählbar/leistbar). Maus+Tastatur teilen sich denselben `selectedIndex` (gleiches Prinzip wie `RewardChoiceUI`).
- **Architektur-Korrektur (wichtig, gefunden beim ersten Hub-Test):** Die Hub↔Dungeon-Verbindung war ursprünglich ein normales, bidirektionales `Door`-Paar (Wiederverwendung von `switchRoom()`) - das widersprach der Entscheidung "Run-Ende: nur Tod", da eine begehbare Tür einen jederzeit nutzbaren Rückweg aus dem Run geschaffen hätte (verstärkt durch die automatische Tür-Entriegelung bei Raum-Clear, die uniform für alle Türen im Raum gilt). Ersetzt durch zwei neue, vom Door-System unabhängige Objekt-Typen: **`PlayerSpawn`** (fester Einstiegspunkt, je einer pro Hub/Run-Startraum) und **`RunEntrance`** (nur im Hub, Auto-Trigger wie `Door`, ruft aber `Main.startRun()` auf statt `switchRoom()`). `Main.startGame()` erstellt einen frischen `Player` und lädt den Hub; `startRun()` lädt den Dungeon OHNE den Player neu zu erstellen. Eine gemeinsame `Main.enterRoom(Room, Vector2)`-Hilfsmethode bündelt die Aufräum-/Aufbau-Logik für `startGame()`/`startRun()`/`switchRoom()`. Kein begehbarer Rückweg aus einem Run mehr möglich - der einzige geplante Ausstieg ist eine spätere Pause-Menü-Option "Run aufgeben" (siehe Backlog unten).
- **Terminal:** Zu `Door` analoge Klasse (Position, `isPlayerInRange()`), aber kein Auto-Trigger - erfordert einen expliziten Tastendruck (neues `GameSettings.interactKey`, Standard `E`).
- Gefundener Bug beim ersten Test: `RunEntrance` war anfangs nur eine unsichtbare `Vector2`-Trigger-Zone ohne eigene Grafik (anders als `Door`/`Terminal`) - zu einer vollwertigen Klasse mit Platzhalter-Textur (orange eingefärbt) aufgewertet.
- Debug-Stats (siehe Sidequest 1.4) zeigen jetzt zusätzlich den aktuellen Stand beider Währungen an - ohne sichtbaren Zähler war beim Testen nicht überprüfbar, ob Sammeln/Speichern tatsächlich funktioniert.

**Backlog (gefunden beim Testen von Quest 9.6, bewusst zurückgestellt): Aktuelle HP nach Hub-Kauf.** `MaxHealthUpgrade.apply()` erhöht aktuell nur `maxHealth`, nicht die aktuelle `health` - im Hub bleibt die aktuelle HP nach einem Kauf unverändert, sie wird erst beim nächsten `Player`-Konstruktor-Aufruf (Run-Start) auf den neuen `maxHealth`-Wert gesetzt. Zu klären: soll ein Hub-Kauf die aktuelle HP sofort mitanheben, oder ist "wirkt erst ab dem nächsten Run" das gewollte Verhalten?

**Backlog (vermerkt während der Planung, bewusst NICHT Teil von Quest 9): Pause-Menü mit "Run aufgeben".** Einziger geplanter manueller Ausstieg aus einem laufenden Run (neben Tod) - soll sich exakt wie ein Tod verhalten (zurück in den Hub). Aktuell nicht umgesetzt, da kein Pause-Menü existiert.

**Backlog (vermerkt während der Planung, bewusst NICHT Teil von Quest 9): Run-interne, nicht-persistente Dritt-Währung.** Idee für spätere In-Run-Shops (z.B. Gold, das mit dem Run endet) - eigenständiges späteres Thema, unabhängig von den beiden Meta-Währungen hier.

## ✅ Sidequest 2: Debug-Werkzeuge
Beim Testen der Quest-9-Persistenz (Käufe/Währung) wurde klar, dass die reine Lese-Anzeige aus Sidequest 1.4 nicht reicht, um Stände gefahrlos zu testen - man müsste sonst die `save.json` von Hand editieren. **Abgeschlossen 2026-08-02.**
- **Fertig wenn:** Ein per Tastendruck ein-/ausblendbares Debug-Menü erlaubt es, alle in der Debug-Stat-Anzeige gezeigten Werte direkt im Spiel anzupassen, plus ein Weg, Terminal-Käufe und Währung gezielt zurückzusetzen.
- **`DebugValue`** (neu) - EIN generischer Wrapper (Label, `Supplier<Float>`-Getter, `Consumer<Float>`-Setter, Schrittweite, Format-Pattern, bei Konstruktion gesnapshotteter Default für `reset()`) statt einer Unterklasse pro Wert - gleiches Prinzip wie `CurrencyPickup` (Quest 9): kein Verhaltensunterschied zwischen den Einträgen, nur welcher Wert gelesen/geschrieben wird. Int-Felder (`projectileCount`) laufen über eine konvertierende Lambda statt einer eigenen Int-Variante.
- **`DebugMenuUI`** (neu) - anders als `RewardChoiceUI`/`UpgradeShopUI` eine VERTIKALE Liste statt nebeneinanderliegender Karten (zu viele Einträge für eine Reihe in 640px Breite). Pro Zeile ein `[-]`/`[+]`-Button (Hover-Highlight, Klick), dazu ein globaler "Alles zurücksetzen"-Button und ein "X"-Schließen-Button (zusätzlich zu Escape und der Toggle-Taste selbst).
- **Öffnen/Schließen:** Neue `GameSettings.debugMenuToggleKey` (Default `F1`). Der Toggle-Check läuft UNCONDITIONAL ganz oben in `Main.render()` (wie der bestehende Fullscreen-Toggle) - funktioniert also auch während `activeReward`/`activeShop` offen sind. Das Debug-Menü ist der höchstpriorisierte Zweig der Pause-Gate-Kette, pausiert also alles Übrige.
- **Neue Setter in `Player.java`** (vorher nur Getter): `setHealth`/`setMaxHealth`/`setSpeed`/`setDashCooldownRemaining`, jeweils sinnvoll geklemmt (z.B. `health` auf `[0, maxHealth]`, sonst würde `Healthbar`s Prozent-Berechnung über 100% hinauslaufen).
- **Enthaltene Werte:** HP, Max-HP, Speed, Dash-Cooldown-Rest, alle relevanten `PlayerStats`-Multiplikatoren (Dmg/AtkSpd/CDR/Crit/CritDmg/ProjectileCount), beide Meta-Währungen. Level/XP sind bewusst NICHT enthalten - Level hängt über eine Formel von der aktuellen XP ab, direktes Setzen würde die XP-Schwelle inkonsistent machen (Entscheidung während der Planung).
- **"Alles zurücksetzen"** setzt jeden Debug-Wert auf seinen Ausgangswert zurück UND macht Terminal-Käufe rückgängig (`purchasedUpgradeIds.clear()`, beide Währungen auf 0, `MetaProgress.save()`) - deckt genau den ursprünglichen Wunsch ab (Käufe testweise rückgängig machen, ohne `save.json` von Hand zu editieren).

## ⬜ Quest 10: Der erste Boss
Ein Boss-Encounter mit eigenem Angriffsmuster/Phasen.
- Fertig wenn: Boss besiegbar, hat mind. 2 unterscheidbare Angriffsmuster.
- **Vorentscheidungen (vermerkt 2026-08-02, Ausblick-Gespräch nach Quest 9, noch nicht umgesetzt):**
  - Boss wird eine **neue, eigenständige Klasse** statt einer Erweiterung/Vererbung der bestehenden `Enemy`-Klasse - mehr Freiheit für Phasen-Logik, kein Risiko dass `Enemy` durch Boss-Spezialfälle unübersichtlich wird.
  - "Mind. 2 unterscheidbare Angriffsmuster" heißt **beides kombiniert**: mehrere gleichzeitig verfügbare Muster UND ein Wechsel des verfügbaren Musters bei HP-Schwellen (klassischer Phasen-Rhythmus plus situative Variation innerhalb einer Phase).
  - Boss bekommt **erst einen Platzhalter**, keine eigene Kunst von Anfang an (gleiches Vertical-Slice-Prinzip wie bisher bei Waffen/Türen) - Movesets/Balancing sollen zuerst stehen.

## ⬜ Quest 11: Das Chaos ordnet sich (Prozedurale Generierung)
Erste Version der prozeduralen Raumkombination, aufbauend auf dem Pool handgebauter Räume aus Quest 4–6 (Ansatz wird dann final entschieden, siehe GDD 4).
- Fertig wenn: Ein Run besteht aus zufällig zusammengesetzten Räumen aus dem bestehenden Raum-Pool.
- Technischer Vorlauf (vermerkt 2026-07-13): Quest 6s `Door.targetRoom`/`targetDoorName` sind fest verdrahtete Verknüpfungen, passend für handgebaute, feste Level — aber NICHT direkt geeignet für einen austauschbaren Raum-Pool (der Generator soll ja zur Laufzeit einen beliebigen kompatiblen Raum wählen, nicht einen fest vorgegebenen). Die Tür-Namenskonvention (Himmelsrichtungen, `"north"`/`"south"`/...) bleibt aber relevant und wird bewusst schon jetzt konsequent genutzt — vermutlich reicht die Richtung allein als Kompatibilitäts-Kriterium für den Pool-Mechanismus, `targetRoom`/`targetDoorName` bleiben parallel für fest verankerte Räume (z.B. Startraum) bestehen. Endgültiger Ansatz erst hier entscheiden, nicht vorher raten (siehe Weapon-Interface-Präzedenzfall).
- **Vorentscheidung (vermerkt 2026-08-02):** Der aktuelle Raum-Pool (`testmap.tmx`, `test01_east.tmx`, `test01_west.tmx`, `testfinal.tmx`) reicht laut Nutzer nicht für einen sinnvollen ersten Test - vorher sollen mehr Räume gebaut werden. Das setzt aber voraus, dass Türen überhaupt erreichbar sind (siehe die unten stehende, aus Sidequest 1 zurückgestellte Tür-Kollisions-Lücke) - **Reihenfolge vor Quest 11:** (1) Zwischenlösung für Tür-Erreichbarkeit, (2) mehr Dungeon-Räume bauen, (3) erst dann Quest 11 selbst angehen.
- **Schritt 1 erledigt (2026-08-04):** `Door.INTERACTION_RADIUS` von 20px auf 48px erhöht (Türen liegen in der 32px dicken Wand-Kachelreihe, die Wand-Kollision aus Sidequest 1 ließ den Spieler vorher nicht nah genug heran). Dabei gefundener Folgebug: `ENTRY_OFFSET_DISTANCE` (32px) war kleiner als der neue Radius - der Spieler landete beim Raumbetreten sofort wieder im Trigger-Bereich derselben Tür. In Räumen ohne eigene Gegner (`test01_west.tmx`/`test01_east.tmx`, 0 `EnemySpawn`-Objekte) fehlte die "Gegner noch am Leben"-Sperre, die das sonst kurz verhindert - wirkte wie ein Endlos-Reload desselben Raums. Offset auf 64px erhöht (Sicherheitsabstand über dem Radius). Beide Werte bewusst grob/vorläufig, endgültige Lösung kommt mit dem Tür-Art-Pass.
- **Zusätzlich umgesetzt (2026-08-04, beim Testen der Tür-Fixes gewünscht):** Räume merken sich jetzt innerhalb eines Runs, ob sie bereits geleert wurden (`Main.clearedRoomPaths`, Schlüssel = `Room.getTmxPath()`) - Backtracking durch den Raum-Graphen spawnt keine Gegner mehr neu, wird bei jedem neuen Run geleert. Bewusst pfad- statt instanz-basiert (siehe Quest-6-Backlog-Punkt unten) - eine instanz-basierte Lösung würde voraussetzen, bereits zu wissen, wie Quest 11 Raum-Vorlagen wiederverwendet.

---
Diese Liste ist ein lebendes Dokument — Reihenfolge/Inhalt kann sich anpassen, sobald wir tiefer in einzelne Quests einsteigen.
